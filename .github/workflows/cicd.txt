name: run install playbook
on: 
    push:
      branches: [ main ]
jobs:
    install:
      runs-on: self-hosted
      steps:
        
        - name: git checkout
          uses: actions/checkout@v4
        
        - name: run playbook
          run: ansible-playbook install-playbook.yml